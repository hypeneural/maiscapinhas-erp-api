# ==============================================================================
# MaisCapinhas ERP API - Environment Configuration
# ==============================================================================
# Copy this file to .env and adjust values for your local environment.
# For Laragon Windows setup, most defaults should work out of the box.
# ==============================================================================

APP_NAME="MaisCapinhas ERP API"
APP_ENV=local
APP_KEY=
APP_DEBUG=true

# Use one of these options:
# Option A: artisan serve
APP_URL=http://localhost:8000
# Option B: Laragon virtual host
# APP_URL=http://maiscapinhas-erp-api.test

APP_TIMEZONE=America/Sao_Paulo
APP_LOCALE=pt_BR
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=pt_BR

# ==============================================================================
# LOGGING - JSON format for production, stack for local
# ==============================================================================
LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# ==============================================================================
# DATABASE - MySQL (Laragon)
# ==============================================================================
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=maiscapinhas_erp
DB_USERNAME=root
DB_PASSWORD=
DB_TIMEZONE=+00:00
DB_CHARSET=utf8mb4

# DB_COLLATION - omit to use MySQL 8 default (utf8mb4_0900_ai_ci)

# ==============================================================================
# CACHE & SESSIONS - File (default) or Redis (if installed)
# ==============================================================================
# Default (works without Redis)
CACHE_STORE=file
SESSION_DRIVER=file

# Production API (recommended with Redis local-only):
# CACHE_STORE=redis
# SESSION_DRIVER=file
# CACHE_PREFIX=mc_api_cache_

# ==============================================================================
# QUEUE - Sync (default) or Redis (if installed)
# ==============================================================================
# Default (runs jobs immediately - works without Redis)
QUEUE_CONNECTION=sync

# Production webhook ingestion (recommended):
# QUEUE_CONNECTION=redis
# QUEUE_FAILED_DRIVER=database-uuids

# ==============================================================================
# PDV WEBHOOK
# ==============================================================================
# Auth mode: hmac | bearer | auto | none
PDV_AUTH_MODE=auto
# Safety fuse for temporary auth_mode=none in production
PDV_ALLOW_NONE_MODE_IN_PRODUCTION=false
# Shared HMAC secret for /api/v1/pdv/sync
PDV_HMAC_SECRET=
# Transitional fallback for legacy agent auth (Bearer token)
PDV_ALLOW_BEARER_FALLBACK=false
PDV_BEARER_TOKEN=
# Timestamp mode: strict | tolerant
PDV_TIMESTAMP_MODE=tolerant
# Supported schema versions (comma separated)
PDV_SUPPORTED_SCHEMA_VERSIONS=2.0,3.0
# Optional JSON Schema validation at ingress
PDV_JSON_SCHEMA_VALIDATION_ENABLED=false
PDV_JSON_SCHEMA_FILE_2_0=docs/schema_v2.0.json
PDV_JSON_SCHEMA_FILE_3_0=docs/schema_v3.0.json
# Dedicated webhook logs
PDV_LOG_CHANNEL=pdv
PDV_LOG_LEVEL=debug
PDV_LOG_DAILY_DAYS=30
PDV_LOG_PAYLOAD_ON_VALIDATION_ERROR=true
PDV_LOG_PAYLOAD_MAX_CHARS=6000
# 10 minutes
PDV_TIMESTAMP_TOLERANCE_SECONDS=600
# Timezone used when payload datetime has no offset
PDV_NAIVE_DATETIME_TIMEZONE=America/Sao_Paulo
# If true, block sync when mapped store alias diverges from payload alias
PDV_BLOCK_ON_ALIAS_MISMATCH=false
# Webhook rate limit per minute
PDV_RATE_LIMIT_PER_MINUTE=180
# Queue used by PDV processing jobs
PDV_QUEUE_NAME=pdv
# Lock duration per store while processing jobs
PDV_STORE_LOCK_SECONDS=30
# Expected worker timeout (Toolkit/worker runtime)
PDV_WORKER_TIMEOUT_SECONDS=180
# Cron-friendly queue consumer (shared hosting)
PDV_CRON_QUEUE_CONSUMER_ENABLED=false
PDV_CRON_QUEUE_CONSUMER_MAX_TIME=50
PDV_CRON_QUEUE_CONSUMER_SLEEP=1
PDV_CRON_QUEUE_CONSUMER_MEMORY=256
PDV_QUEUE_CONSUMER_HEARTBEAT_CACHE_KEY=pdv:queue-consumer:heartbeat
# Alert threshold for queued syncs not consumed
PDV_QUEUE_STALE_THRESHOLD_MINUTES=20
# Job retry policy for ProcessPdvSyncJob
PDV_JOB_TRIES=5
PDV_JOB_BACKOFF_SECONDS=10,30,60,120
# RAW payload retention (days)
PDV_RAW_RETENTION_DAYS=30
# Auto-retry failed syncs via scheduler
PDV_RETRY_FAILED_ENABLED=false
PDV_RETRY_FAILED_LIMIT=200
PDV_RETRY_FAILED_MAX_ATTEMPTS=8
PDV_RETRY_FAILED_OLDER_THAN_MINUTES=15
# Ops monitor (PR-21)
PDV_MONITOR_ENABLED=true
PDV_MONITOR_MAX_QUEUE_BACKLOG=3
PDV_MONITOR_MAX_QUEUED_SYNCS=5
PDV_MONITOR_MAX_FAILED_JOBS=0
# silent store = mapped store with no sync above threshold
PDV_MONITOR_SILENT_STORE_THRESHOLD_MINUTES=120
# tolerated amount before alerting (0 = any stale store triggers alert)
PDV_MONITOR_MAX_STALE_STORES=0
PDV_MONITOR_ALERT_COOLDOWN_MINUTES=30
# External alerts (configure one or more channels)
PDV_MONITOR_ALERT_WEBHOOK_URL=
PDV_MONITOR_ALERT_SLACK_WEBHOOK_URL=
PDV_MONITOR_ALERT_EMAILS=
PDV_MONITOR_STATE_CACHE_KEY=pdv:ops-monitor:state

# ==============================================================================
# REDIS CONFIGURATION
# ==============================================================================
REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379
REDIS_DB=0
REDIS_CACHE_DB=1
REDIS_PREFIX=mc:api:
# Queue tuning (retry_after must be > worker timeout)
REDIS_QUEUE_CONNECTION=default
REDIS_QUEUE=default
REDIS_QUEUE_RETRY_AFTER=300
REDIS_QUEUE_BLOCK_FOR=5
# Cache store tuning
REDIS_CACHE_CONNECTION=cache
REDIS_CACHE_LOCK_CONNECTION=default

# ==============================================================================
# SESSION CONFIGURATION
# ==============================================================================
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=localhost

# ==============================================================================
# SANCTUM - SPA Authentication
# ==============================================================================
# Stateful domains for cookie-based SPA auth (React frontend)
SANCTUM_STATEFUL_DOMAINS=localhost:5173,127.0.0.1:5173,localhost:8000,127.0.0.1:8000,maiscapinhas-erp-api.test

# ==============================================================================
# CORS - Frontend URL
# ==============================================================================
FRONTEND_URL=http://localhost:5173

# ==============================================================================
# MAIL (configure when needed)
# ==============================================================================
MAIL_MAILER=log
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# ==============================================================================
# AWS (configure when deploying)
# ==============================================================================
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# ==============================================================================
# VITE (for asset compilation if needed)
# ==============================================================================
VITE_APP_NAME="${APP_NAME}"
